import 'package:flutter/material.dart';

class EppisodeScreen extends StatelessWidget {
  const EppisodeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}